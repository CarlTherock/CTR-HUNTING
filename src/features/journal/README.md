# features/journal

**Status:** placeholder UI only — full implementation planned for **Phase
13**.

Field journal: observations, photos, positions, conditions and traces over
time, all viewable on the map. Built on top of `features/observations/`,
`features/photos/` and `features/history/`.
