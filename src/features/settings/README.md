# features/settings

**Status:** functional (Phase 0), minimal.

App-level settings backed by `src/database/settingsRepository.ts`. Only
theme/about/PWA-install info exists in Phase 0; per-feature settings are
added as those features ship.
