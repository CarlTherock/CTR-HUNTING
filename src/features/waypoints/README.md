# features/waypoints

**Status:** placeholder UI only — full implementation planned for **Phase 2**.

Create/edit/move/delete waypoints, categorize, attach notes and photos, and
persist locally (see `src/database/db.ts`). Track recording lives in the
sibling `features/tracks/` module.
