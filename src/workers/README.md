# workers/

Web Workers for CPU-heavy work that must not block the UI thread — terrain
analysis (Phase 8/9), track simplification, image processing (Phase 12).

Empty in Phase 0: no feature needs offloaded computation yet. Added when the
first such feature is implemented.
